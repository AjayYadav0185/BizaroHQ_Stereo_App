name: car_stereo_launcher
description: "Production-ready Android BizaroHQ Stereo optimized for landscape head units."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  geolocator: ^14.0.3
  intl: ^0.20.3
  http: ^1.2.0
  cupertino_icons: ^1.0.8
  connectivity_plus: ^6.1.4
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/models/car.glb
